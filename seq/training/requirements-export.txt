# Optional — зөвхөн TFJS export (train.py дараа нэг удаа)
# Суулгах:
#   python3 -m pip install --no-compile --no-deps tensorflowjs==4.13.0
#   python3 -m pip install --no-compile h5py>=3.10 six
tensorflowjs==4.13.0
h5py>=3.10
six
